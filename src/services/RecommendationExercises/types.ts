export type Filters = {
  category: string;
  level: string;
  days_per_week: string;
  duration: string;
};

export type Plan = {
  description: string;
  exercise_count: number;
  plan_id: number;
  plan_name: string;
};
